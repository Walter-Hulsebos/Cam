using System.Collections;
using System.Collections.Generic;
using Lean.Touch;
using UnityEngine;

public class PanelIsShowing : MonoBehaviour
{
    /*BoutiquesTextUI text;
    [SerializeField] private GameObject panel;

    private void Awake()
    {
        text = GameObject.Find("TestText").GetComponent<BoutiquesTextUI>();
    }
    private void Update()
    {
        if (text.isShowing) {
            ShowPanel();
        }
        else if (text.isShowing == false)
        {
            HidePanel();
        }
        
    }

    private void ShowPanel()
    {
        panel.SetActive(true);
    }
    private void HidePanel()
    {
        panel.SetActive(false);
    }*/
   /* LeanSelectByFinger selectByFinger;
    [SerializeField] private GameObject panel;
    private void Awake()
    {
        selectByFinger = GameObject.Find("TapToSelect").GetComponent<LeanSelectByFinger>();
    }
    private void Update()
    {
        if (LeanSelectByFinger.LeanSelectableEvent. != null)
        {

        }*/


    }
